# UNTrade Frontend

Next.js 14 App Router UI for the UNTrade marketplace API.

## Run

```bash
cd frontend
cp .env.local.example .env.local   # if needed
npm install
npm run dev
```

App: http://localhost:3000  
API: http://127.0.0.1:8001/api/v1
